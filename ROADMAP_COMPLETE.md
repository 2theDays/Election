# 2026 충청북도 도지사 선거 후보 관계망 분석 프로그램
## 실행 가능한 통합 로드맵 (30주 완성 계획)

---

## 📌 프로젝트 개요

**목적**: 충북 도지사 후보 6명의 정치 네트워크를 분석하고 시스템 다이내믹스로 선거 결과를 예측

**대상 후보**:
1. 신용한 (더불어민주당, 지방시대위 부위원장)
2. 노영민 (더불어민주당, 前 대통령비서실장)
3. 송기섭 (더불어민주당, 진천군수)
4. 한범덕 (더불어민주당, 前 청주시장)
5. 윤희근 (국민의힘, 前 경찰청장)
6. 이종배 (국민의힘, 국회의원)

**차별화 포인트**:
✅ 뉴스 자동 수집 + LLM 관계 추출
✅ R igraph 네트워크 분석
✅ Vensim + deSolve 병행 SD 모델링
✅ 충북 지역 특성 반영 (지역주의, 계파 분석)

---

## 📅 전체 타임라인 (7개월, 30주)

```
2025.12 - 2026.07: 시스템 개발
2026.07 - 2026.06: 실전 운영 (선거일까지)
```

---

## 🔧 Phase 1: 데이터 인프라 구축 (Week 1-4)

### **목표**: 뉴스 자동 수집 및 관계 추출 시스템 완성

### **주요 작업**

#### Week 1: 크롤링 시스템 개발
```bash
# Python 환경 설정
pip install requests beautifulsoup4 anthropic pandas --break-system-packages

# 실행
python /home/claude/news_crawler.py
```

**산출물**:
- `news_crawler.py`: 네이버 뉴스 자동 수집
- `candidates_data.json`: 후보자 기본 정보

**주요 기능**:
- 네이버 뉴스 검색 API 활용
- 키워드: "{후보명} 충북도지사", "충북도지사 선거" 등
- 수집 기간: 최근 30일
- 기사당 평균 수집: 30개

#### Week 2: Claude API 관계 추출
```python
# 환경변수 설정
export ANTHROPIC_API_KEY="your-api-key-here"

# Claude에게 분석 요청
# 출력: relationships_raw.csv
```

**추출 관계 유형**:
- 정치적동맹
- 경쟁
- 학연 (동문)
- 지연 (출신지)
- 사제 (스승-제자)
- 협력 / 비판 / 지지

#### Week 3: 데이터 품질 검증
- 샘플링 검수 (100개 중 20개 수동 확인)
- 정확도 목표: 85% 이상
- 오류 패턴 분석 및 프롬프트 개선

#### Week 4: PostgreSQL 데이터베이스 구축
```sql
CREATE TABLE relationships (
    id SERIAL PRIMARY KEY,
    person1 VARCHAR(50),
    person2 VARCHAR(50),
    relation_type VARCHAR(50),
    strength FLOAT,
    evidence TEXT,
    date DATE,
    url VARCHAR(500)
);

CREATE INDEX idx_person ON relationships(person1, person2);
```

**자동화 설정**:
```bash
# Cron job (매일 새벽 3시 실행)
0 3 * * * cd /home/claude && python news_crawler.py >> logs/crawler.log 2>&1
```

---

## 📊 Phase 2: 네트워크 분석 (Week 5-8)

### **목표**: R igraph로 관계망 구조 분석

### **주요 작업**

#### Week 5: 기본 네트워크 구축
```r
# R 패키지 설치
install.packages(c("igraph", "tidyverse", "visNetwork", "multinet"))

# 실행
source("/home/claude/network_analysis.R")
```

**핵심 지표 계산**:
1. **연결 중심성** (Degree): 인맥 수
2. **매개 중심성** (Betweenness): 킹메이커 역할 ⭐⭐⭐
3. **근접 중심성** (Closeness): 정보 확산 속도
4. **페이지랭크** (PageRank): 실질적 영향력
5. **아이겐벡터 중심성**: 영향력 있는 사람과의 연결

#### Week 6: 커뮤니티 탐지 (파벌 분류)
```r
# Louvain 알고리즘
communities <- cluster_louvain(g)

# 예상 결과:
# - 커뮤니티 1: 청주 민주당 그룹 (신용한, 한범덕, 노영민)
# - 커뮤니티 2: 진천 행정관료 그룹 (송기섭)
# - 커뮤니티 3: 국민의힘 그룹 (윤희근, 이종배)
```

**Modularity 목표**: 0.4 이상 (명확한 파벌 구조)

#### Week 7: 다층 네트워크 분석
```r
library(multinet)

# Layer 1: 공식 관계 (정당, 직함)
# Layer 2: 비공식 관계 (학연, 지연)
# Layer 3: 디지털 관계 (SNS)

layer_correlation_ml(ml, "official", "informal")
```

**분석 목표**:
- 공식 동료이면서 동향인 경우 → 결속도 ↑
- 청주고 동문 네트워크 강도 측정

#### Week 8: 지역별 네트워크 구조
```r
# 충북 11개 시군별 분석
regions <- c("청주", "충주", "진천", "음성", "제천", "단양", 
            "괴산", "증평", "영동", "옥천", "보은")

for(region in regions) {
  regional_density <- calculate_density(region)
  regional_betweenness <- avg_betweenness(region)
}
```

**산출물**:
- `centrality_scores.csv`: 중심성 점수표
- `influence_index.csv`: 선거 영향력 지수
- `network_visualization.html`: 인터랙티브 시각화

---

## 🔄 Phase 3: 시스템 다이내믹스 모델링 (Week 9-14)

### **목표**: Vensim + deSolve 병행 시뮬레이션 시스템

### **A. Vensim 모델 (Week 9-11)**

#### Week 9: 모델 구조 설계
```
Stock (저량):
- 지지층_신용한, 지지층_노영민, 지지층_송기섭, 지지층_한범덕
- 무당층
- 포기층

Flow (유량):
- 유입률 = f(매개중심성, 페이지랭크, SNS긍정도, 네트워크밀도)
- 이탈률 = f(스캔들강도, 네트워크결속도)
- 포기률 = 정치혐오지수
```

**핵심 방정식**:
```
지지율_변화 = (무당층_유입 - 이탈 - 포기) * dt

무당층_유입 = 무당층 * (0.3*매개중심성 + 0.4*페이지랭크 + 
                       0.2*SNS긍정 + 0.1*네트워크밀도)
```

#### Week 10: 파라미터 Calibration
- 현재 여론조사 결과와 비교
- 과거 선거 데이터 (2018, 2022)로 검증
- R² > 0.8 목표

#### Week 11: 시나리오 설계
1. **기본 시나리오**: 현상 유지
2. **스캔들 시나리오**: 특정 후보 스캔들 발생 (강도별)
3. **단일화 시나리오**: A-B 후보 단일화
4. **중앙 지원 시나리오**: 대통령/당대표 지지 선언
5. **복합 시나리오**: 스캔들 + 단일화

### **B. R deSolve 모델 (Week 12-14)**

#### Week 12: deSolve 구현
```r
library(deSolve)

# Vensim과 동일한 로직을 R로 변환
election_dynamics <- function(t, state, parameters) {
  # ... 미분방정식 정의 ...
}

# 시뮬레이션 실행
out <- ode(y = initial_state, times = times, 
          func = election_dynamics, parms = parameters)
```

**장점**:
- R 생태계 완전 통합
- 통계 분석 즉시 연결
- Git 버전 관리
- Shiny 앱 연동 용이

#### Week 13: 민감도 분석
```r
# 파라미터 범위별 결과 변화
sensitivity_analysis <- function(param_name, range) {
  results <- list()
  for(value in range) {
    params[param_name] <- value
    out <- ode(...)
    results[[value]] <- out
  }
  return(results)
}
```

**분석 대상**:
- 매개중심성 ±20% 변화 → 지지율 변화?
- 스캔들 강도 0.1 ~ 1.0 → 당선 확률?

#### Week 14: Vensim ↔ deSolve 결과 비교
- 동일 조건에서 두 모델 결과 비교
- 차이 < 5% 목표
- 발산 시 파라미터 재조정

---

## 🇰🇷 Phase 4: 한국 정치 맥락 특화 (Week 15-18)

### **목표**: 지역주의, 계파, 중앙 정치 영향력 모델링

### **Week 15: 지역주의 패턴 분석**

```r
# 충북 11개 시군별 지도 시각화
library(sf)
library(ggplot2)

chungbuk_map <- st_read("data/chungbuk_shapefile.shp")

# 지역별 네트워크 밀도 매핑
ggplot(chungbuk_map) +
  geom_sf(aes(fill = network_density)) +
  scale_fill_gradient(low = "lightblue", high = "darkred")
```

**분석 지표**:
- 청주 vs 비청주 네트워크 차이
- 충주권, 제천권 독자성
- 음성/진천/증평 결속도

### **Week 16: 당내 계파 자동 분류**

```r
# Louvain 결과 해석
for(community in communities) {
  # 공통 속성 분석
  common_attrs <- analyze_attributes(community)
  
  # 계파 명명
  if(이념 == "보수" & 지역 == "청주") {
    faction_name <- "청주 보수 실용파"
  }
}
```

**예상 계파**:
- 친문계 (노영민 중심)
- 친명계 (신용한, 송기섭?)
- 비명비문 중도실용 (한범덕)
- 국민의힘 보수 (윤희근, 이종배)

### **Week 17: 중앙 정치 영향력**

```r
# 대통령/당대표와의 네트워크 거리
central_politicians <- c("이재명", "한동훈", "조국")

for(candidate in 충북_후보) {
  path_length <- shortest.paths(g, candidate, central_politicians)
  
  # 2-hop 이내 = 핵심 측근
  # 3-hop 이상 = 주변부
}
```

**SD 모델 반영**:
```
중앙_영향력_보정 = IF 최단경로 <= 2
                  THEN 기본_지지율 * 1.3
                  ELSE 기본_지지율 * 0.8
```

### **Week 18: 공천 시나리오 자동화**

```r
scenarios <- list(
  "A후보 전략공천" = function() {
    modify_parameters(...)
    run_simulation()
  },
  "B-C 단일화" = function() {
    merge_supporters(...)
    run_simulation()
  }
)

# 모든 시나리오 실행 및 비교
results <- lapply(scenarios, function(f) f())
compare_scenarios(results)
```

---

## 💻 Phase 5: Shiny 대시보드 개발 (Week 19-24)

### **목표**: 웹 기반 인터랙티브 분석 도구

### **Week 19-20: UI/UX 설계**

**메인 페이지 구조**:
```
┌─────────────────────────────────────┐
│  2026 충북 도지사 선거 분석 시스템   │
├─────────────────────────────────────┤
│  [네트워크 탐색] [후보 프로필]      │
│  [시뮬레이션] [지역 분석]            │
│  [시나리오 비교] [리포트 생성]       │
└─────────────────────────────────────┘
```

### **Week 21: 네트워크 탐색 탭**

```r
# 인터랙티브 네트워크
visNetworkOutput("network_plot", height = "700px")

# 컨트롤
selectInput("layer", "레이어", choices = c("전체", "공식", "비공식", "SNS"))
sliderInput("time", "시점", min = 1, max = 24, value = 12)
selectInput("highlight", "강조", choices = c("매개중심성", "페이지랭크"))
```

### **Week 22: 시뮬레이션 탭**

```r
# 파라미터 조정
sliderInput("scandal_A", "후보 A 스캔들", 0, 1, 0, step = 0.1)
checkboxInput("unity_AB", "A-B 단일화", FALSE)
dateInput("scandal_date", "스캔들 발생일")

# 실시간 시뮬레이션
observeEvent(input$run_sim, {
  results <- run_deSolve_simulation(
    scandal_A = input$scandal_A,
    unity = input$unity_AB
  )
  
  output$support_timeseries <- renderPlotly({
    plot_ly(results, x = ~time) %>%
      add_lines(y = ~support_A, name = "A")
  })
})
```

### **Week 23: 지역 분석 탭**

```r
# 충북 지도
leafletOutput("chungbuk_map", height = "600px")

# 지역별 통계
tableOutput("regional_stats")
plotOutput("regional_density")
```

### **Week 24: 리포트 자동 생성**

```r
# R Markdown 템플릿
downloadHandler(
  filename = "선거분석_리포트.pdf",
  content = function(file) {
    rmarkdown::render("report_template.Rmd",
                     output_file = file,
                     params = list(
                       network_data = g,
                       simulation_results = results
                     ))
  }
)
```

**리포트 내용**:
1. 요약 (Executive Summary)
2. 네트워크 구조 분석
3. 중심 인물 Top 10
4. 파벌 구조
5. 지지율 예측 (SD 모델)
6. 시나리오별 당선 확률
7. 권장사항

---

## 🔄 Phase 6: 실시간 모니터링 (Week 25-30)

### **목표**: 자동 업데이트 및 알림 시스템

### **Week 25-26: 일일 자동 업데이트**

```python
# daily_update.py
def daily_update_pipeline():
    # 1. 뉴스 크롤링 (최근 24시간)
    new_articles = crawl_last_24h()
    
    # 2. Claude API 분석
    new_relationships = extract_relations(new_articles)
    
    # 3. DB 업데이트
    update_postgresql(new_relationships)
    
    # 4. R 네트워크 재계산
    os.system("Rscript update_network_metrics.R")
    
    # 5. SD 파라미터 자동 조정
    if network_changed_significantly():
        update_sd_parameters()
    
    # 6. 알림 발송
    if detect_critical_change():
        send_email_alert("긴급: 네트워크 구조 급변 감지!")

# Cron: 매일 새벽 3시
# 0 3 * * * python daily_update.py
```

### **Week 27: 이상 패턴 감지**

```r
library(changepoint)

# 급격한 변화 탐지
betweenness_ts <- get_daily_betweenness("신용한")
changepoints <- cpt.mean(betweenness_ts)

if(length(changepoints@cpts) > 0) {
  alert <- paste0(
    "신용한 매개중심성 급변: ",
    changepoints@cpts[1], "일차"
  )
  send_notification(alert)
}
```

### **Week 28-29: 주간 변화 리포트**

```r
# Weekly Report (매주 월요일 발송)
generate_weekly_report <- function() {
  # 1. 지난주 주요 변화
  # 2. 새로 추가된 관계
  # 3. 지지율 변화 추이
  # 4. 주요 뉴스 요약
  # 5. 다음주 예측
}
```

### **Week 30: 최종 점검 및 배포**

- 전체 시스템 통합 테스트
- 보안 점검 (개인정보 비식별화)
- 사용자 매뉴얼 작성
- Shiny 앱 배포 (shinyapps.io 또는 자체 서버)

---

## 📦 최종 산출물

### **1. 소프트웨어**
- ✅ Python 뉴스 크롤러
- ✅ R 네트워크 분석 스크립트
- ✅ Vensim SD 모델 (.mdl)
- ✅ R deSolve SD 모델
- ✅ Shiny 웹 대시보드
- ✅ 자동화 스크립트

### **2. 데이터**
- ✅ 관계망 데이터베이스 (PostgreSQL)
- ✅ 일일 뉴스 아카이브
- ✅ 시계열 네트워크 지표
- ✅ 시뮬레이션 결과 로그

### **3. 문서**
- ✅ 방법론 설명서
- ✅ 사용자 매뉴얼
- ✅ API 문서
- ✅ 주간 분석 리포트 (선거일까지)

### **4. 학술 연구**
- ✅ 학술논문 (선거 후 발표)
  - "충북 지방선거 네트워크 구조와 당선 요인"
  - "시스템 다이내믹스를 활용한 선거 예측 모델"

---

## ⚠️ 리스크 관리

### **데이터 품질**
| 리스크 | 대응 방안 |
|-------|----------|
| 뉴스 편향 | 다양한 출처 (충청투데이, 중도일보, 굿모닝충청) |
| LLM 오류 | 샘플링 검증 (20% 수동 확인) |
| 인과관계 혼동 | 상관관계 명시, 인과는 조심스럽게 해석 |

### **개인정보 보호**
- ✅ 공개 정보만 수집 (뉴스, 공식 발표)
- ✅ 비식별화 처리
- ✅ 사생활 정보 제외

### **정치적 중립성**
- ✅ 알고리즘 투명성 공개
- ✅ 객관적 지표만 사용
- ✅ 편향 검증 테스트

### **법적 검토**
- ✅ 저작권: robots.txt 준수, Fair Use
- ✅ 선거법: 여론조사 규정 확인
- ✅ 데이터 보호법: GDPR/개인정보보호법

---

## 💰 예산 추정 (옵션)

| 항목 | 비용 |
|-----|------|
| Claude API 사용료 | $200 / 월 |
| Shiny 서버 호스팅 | $50 / 월 |
| 데이터베이스 (AWS RDS) | $100 / 월 |
| Vensim PLE | 무료 |
| R / Python | 무료 |
| **총 7개월** | **약 $2,450** |

---

## 🎯 성공 지표 (KPI)

1. **데이터 수집**
   - 일일 뉴스 수집 안정성: 95% 이상
   - 관계 추출 정확도: 85% 이상

2. **네트워크 분석**
   - Modularity (파벌 분리도): 0.4 이상
   - 중심성 지표 일관성: 주간 변동 < 10%

3. **시뮬레이션**
   - 과거 선거 데이터 재현: R² > 0.8
   - 여론조사 대비 오차: ±5%p 이내

4. **실전 예측** (선거 후 검증)
   - 당선자 맞춤: ⭐⭐⭐
   - 득표율 오차: ±7%p 이내 목표

---

## 📚 참고 문헌

1. Wasserman, S., & Faust, K. (1994). *Social Network Analysis*.
2. Sterman, J. (2000). *Business Dynamics: Systems Thinking*.
3. Barabási, A. L. (2016). *Network Science*.
4. 한국선거학회 (2023). "지방선거 투표행태 분석"

---

## 🚀 시작하기

```bash
# 1. Python 환경 설정
pip install requests beautifulsoup4 anthropic pandas --break-system-packages

# 2. R 패키지 설치
R -e "install.packages(c('igraph', 'tidyverse', 'visNetwork', 'deSolve', 'shiny'))"

# 3. API 키 설정
export ANTHROPIC_API_KEY="your-key-here"

# 4. Phase 1 실행
python /home/claude/news_crawler.py

# 5. Phase 2 실행
Rscript /home/claude/network_analysis.R

# 6. Phase 3 실행
Rscript /home/claude/sd_model_deSolve.R
```

---

**프로젝트 시작일**: 2025-12-27
**예상 완료일**: 2026-07-31
**선거일**: 2026-06-03
**실전 운영 기간**: 11개월

---

## ✅ Next Steps

**지금 바로 할 일**:
1. [ ] Week 1: Python 크롤러 프로토타입 테스트 (샘플 10개 기사)
2. [ ] Week 2: Claude API로 관계 추출 검증
3. [ ] Week 3: R igraph 기본 분석 실행
4. [ ] Week 4: 첫 번째 네트워크 시각화 완성

**첫 번째 마일스톤** (4주 후):
- 6명 후보의 기본 관계망 그래프 완성
- 중심성 점수 산출
- 첫 번째 분석 리포트 생성

---

**문의**: 프로젝트 진행 중 질문사항은 언제든지!
